import type { PageInfo } from '@plus-pro-components/types'

export const DefaultPageSizeList: number[] = [10, 20, 30, 40, 50, 100, 200, 300, 400, 500]

export const DefaultPageInfo: PageInfo = {
  page: 1,
  pageSize: 20
}
